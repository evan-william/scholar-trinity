<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>AP Exam Management</title><style>
body{margin:0;background:#f5f7fb;color:#1f2a37;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Arial,sans-serif}header{background:#153764;color:white;padding:18px 24px}.wrap{max-width:1180px;margin:0 auto;padding:22px 16px}.card{background:white;border:1px solid #d9dee8;border-radius:8px;padding:18px;box-shadow:0 4px 16px rgba(22,47,83,.05)}.btn{border:0;border-radius:6px;padding:10px 14px;font-weight:900;text-decoration:none;display:inline-flex;background:#153764;color:white}.btn.light{background:white;color:#153764;border:1.5px solid #d9dee8}.btn.danger{background:#b42318}table{width:100%;border-collapse:collapse}th,td{text-align:left;padding:10px 8px;border-bottom:1px solid #edf0f5;font-size:13px}th{color:#153764}.badge{padding:3px 8px;border-radius:999px;background:#eef3f9;color:#153764;font-size:11px;font-weight:900}.notice{background:#e8f6ef;color:#237a4f;padding:10px 12px;border-radius:8px;margin-bottom:12px}.top{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:14px}@media(max-width:900px){table{display:block;overflow-x:auto}}</style></head><body>
<header><h1>Admin Exam Management</h1><p>Configure AP exam availability, fees, quota, date, time, and display order.</p></header>
<main class="wrap">@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif
<div class="card"><div class="top"><h2>AP Exam Subjects</h2><a class="btn" href="{{ route('admin.ap-exam-subjects.create') }}">Add Subject</a></div>
<table><thead><tr><th>Order</th><th>Season</th><th>Code</th><th>Name</th><th>Category</th><th>Date / Time</th><th>Quota</th><th>Fees</th><th>Status</th><th></th></tr></thead><tbody>
@foreach($subjects as $subject)<tr><td>{{ $subject->sort_order }}</td><td>{{ $subject->examSeason?->season_name ?? 'Legacy' }}</td><td>{{ $subject->code }}</td><td>{{ $subject->name }}</td><td>{{ $subject->category }}</td><td>{{ optional($subject->exam_date)->format('Y-m-d') }} {{ $subject->start_time ? substr($subject->start_time,0,5) : '' }}-{{ $subject->end_time ? substr($subject->end_time,0,5) : '' }}</td><td>{{ $subject->registered_count }}/{{ $subject->quota ?? 'No cap' }}</td><td>{{ $subject->currency }} {{ number_format($subject->exam_fee + $subject->service_fee + $subject->late_registration_fee) }}</td><td><span class="badge">{{ $subject->status }}</span></td><td><a class="btn light" href="{{ route('admin.ap-exam-subjects.edit',$subject) }}">Edit</a><form method="POST" action="{{ route('admin.ap-exam-subjects.destroy',$subject) }}" style="display:inline">@csrf @method('DELETE')<button class="btn danger" type="submit">Disable</button></form></td></tr>@endforeach
</tbody></table>{{ $subjects->links() }}</div></main></body></html>
